package main

import "fmt"

// runOnce and runWatch are fleshed out once the supporting internal
// packages exist (see stage 8 of the implementation plan). These are
// placeholder implementations so the module builds at each intermediate
// stage.
func runOnce(cfg config) {
	fmt.Println("spacelift-notifier: not yet implemented")
}

func runWatch(cfg config) {
	fmt.Println("spacelift-notifier: not yet implemented")
}
